import Head from "next/head";

const HeadChunk = () => (
  <>
    <Head>
      <title>friendzone.</title>
      <meta name="description" content="Connect better." />
      <link rel="icon" href="/favicon.ico" />
    </Head>
  </>
);

export default HeadChunk
