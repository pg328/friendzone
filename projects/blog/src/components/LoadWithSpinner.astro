import { ReactNode } from "react"
import Spinner from "./Spinner"

interface ILoadWithSpinnerProps {
  isLoading: boolean,
  children: ReactNode
}


const LoadWithSpinner = ({ children, isLoading }: ILoadWithSpinnerProps) => <>{isLoading ? <div className="w-full h-full flex justify-center items-center">
  <Spinner />
</div>
  : children}</>

export default LoadWithSpinner
