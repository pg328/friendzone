{
  "name": "friendzone",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "projects/*",
    "packages/*"
  ],
  "scripts": {
    "app": "cd projects/mobile && yarn dev",
    "blog": "turbo run dev --filter blog",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint-config-custom": "*",
    "prettier": "latest",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {},
  "packageManager": "yarn@1.22.10"
}
